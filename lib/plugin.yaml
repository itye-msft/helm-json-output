name: "json"
version: "0.1.0"
usage: "Formats the output of helm commands to json."
description: |-
  Formats the output of helm commands to json.
ignoreFlags: false
useTunnel: false
command: "dotnet $HELM_PLUGIN_DIR/lib/json.dll"